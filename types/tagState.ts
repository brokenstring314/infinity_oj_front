export interface tagState {
    tagId: number,
    tagName: string,
    tagCategory: string,
    createdAt: Date,
    updatedAt: Date
}

export type tagStateRealize = {
    tagId: number
    tagName: string
    tagCategory: string
    createdAt: Date
    updatedAt: Date
}

